# Backend Clean Architecture Rules

This file is kept only for backward-compatible links.

The official backend architecture rules now live at:

- `docs/architecture/clean-architecture-rules.md`

Before changing backend business modules, read that file first. Do not duplicate the full rules here; keeping a single source of truth prevents drift between AI-agent instructions, developer docs, and module plans.
