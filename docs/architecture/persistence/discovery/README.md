# Persistence Discovery

This folder contains discovery evidence reused across the persistence
roadmap: runtime mappings, repository consumers, API flows, state constraints,
module graph, ownership decisions, and baseline inclusion decisions.

`system-overview.md` records the foundational Phase 1 discovery but remains
here because later phases still use it. Phase-only scope and implementation
evidence live under [`../phases`](../phases/README.md).
